---
tag: business-rule
status: active
file_origin: services/ai/openaiService.ts
---

# True se houver chave OpenAI no ambiente (fallback quando o Gemini retorna 429/co

**Descrição:** True se houver chave OpenAI no ambiente (fallback quando o Gemini retorna 429/cota).

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências:**

_Nenhuma entidade tipada detectada automaticamente._
