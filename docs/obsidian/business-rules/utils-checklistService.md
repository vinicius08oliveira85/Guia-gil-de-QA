---
tag: business-rule
status: active
file_origin: utils/checklistService.ts
aggregate: module
---

# Módulo: Checklist Service

**Descrição:** Agregado de `utils/checklistService.ts` com 6 exportação(ões) relevante(s) (funções, const arrow e schemas Zod `*Schema`).

**Exportações analisadas:** 6

## `getChecklistForPhase`

**Descrição:** Regra derivada da exportação `getChecklistForPhase` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

## `getChecklistProgress`

**Descrição:** Regra derivada da exportação `getChecklistProgress` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

## `canMoveToNextPhase`

**Descrição:** Regra derivada da exportação `canMoveToNextPhase` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

## `updateChecklistItem`

**Descrição:** Regra derivada da exportação `updateChecklistItem` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

## `addChecklistItem`

**Descrição:** Regra derivada da exportação `addChecklistItem` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

## `removeChecklistItem`

**Descrição:** Regra derivada da exportação `removeChecklistItem` em `utils/checklistService.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):**

[[ChecklistItem]]

---

**Referências (módulo):**

[[ChecklistItem]]
