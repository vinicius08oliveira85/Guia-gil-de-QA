---
tag: business-rule
status: active
file_origin: services/supabaseCircuitBreaker.ts
aggregate: module
---

# Módulo: Supabase Circuit Breaker

**Descrição:** Agregado de `services/supabaseCircuitBreaker.ts` com 3 exportação(ões) relevante(s) (funções, const arrow e schemas Zod `*Schema`).

**Exportações analisadas:** 3

## `isSupabaseRemotePaused`

**Descrição:** Regra derivada da exportação `isSupabaseRemotePaused` em `services/supabaseCircuitBreaker.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):** _Nenhuma entidade tipada detectada neste export._

---

## `pauseSupabaseRemoteAfterTransientFailure`

**Descrição:** Regra derivada da exportação `pauseSupabaseRemoteAfterTransientFailure` em `services/supabaseCircuitBreaker.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Avaliar condição: `!wasPaused`

**Referências (trecho):** _Nenhuma entidade tipada detectada neste export._

---

## `clearSupabaseRemotePause`

**Descrição:** Regra derivada da exportação `clearSupabaseRemotePause` em `services/supabaseCircuitBreaker.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Revisar o trecho de código de origem para condições explícitas (if/else, early returns, filtros).
- [ ] Confirmar integração com tipos de domínio e serviços referenciados no arquivo.

**Referências (trecho):** _Nenhuma entidade tipada detectada neste export._

---

**Referências (módulo):**

_Nenhuma entidade tipada agregada._
