---
tag: business-rule
status: active
file_origin: hooks/useKeyboardShortcuts.ts
---

# Use Keyboard Shortcuts

**Descrição:** Regra derivada da exportação `useKeyboardShortcuts` em `hooks/useKeyboardShortcuts.ts`: lógica e validações implementadas no código.

**Lógica Aplicada:**

- [ ] Avaliar condição: `ctrlMatch && shiftMatch && altMatch && keyMatch`

**Referências:**

_Nenhuma entidade tipada detectada automaticamente._
