# Copie para .env e preencha os valores.
# Nunca commite o arquivo .env.

# Figma (opcional)
VITE_FIGMA_API_TOKEN=
FIGMA_FILE_KEY=
STORYBOOK_FIGMA_URL=
CHROMATIC_PROJECT_TOKEN=

# Supabase – desenvolvimento local com npm run dev:local
# Proxy na mesma origem quando você abre http://localhost:3000
VITE_SUPABASE_PROXY_URL=/api/supabaseProxy

# Obrigatório para o proxy Supabase (api/supabaseProxy.ts) – use no .env local
# Obtenha em: Supabase → Project Settings → API
SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# Alternativa: SDK direto no cliente (sem proxy)
# VITE_SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=sua-anon-key-aqui
