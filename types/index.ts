/**
 * Ponto de entrada opcional para o contrato de tipos do app.
 * O modelo principal continua em `types.ts` na raiz; imports como `from '../types'`
 * resolvem para esse arquivo. Aqui apenas re-exportamos para quem preferir `types/index`.
 */
export * from '../types';
