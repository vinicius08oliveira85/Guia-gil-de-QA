/**
 * @deprecated Backlog integrado em TasksView (subvisão "Backlog" na aba Tarefas & Testes).
 * Mantido apenas para evitar imports quebrados; use TasksView com listMode="backlog".
 */
export { TasksView as BacklogView } from './TasksView';
